# Arcane Arsenal

Fabric 26.2 project bootstrap.

## Automatic build
Every push to `main` runs `.github/workflows/build.yml` using Java 25.
When the Actions run succeeds, open the run and download the
`Arcane-Arsenal-26.2-Fabric` artifact.

This bootstrap intentionally contains only original Arcane Arsenal code/assets.
